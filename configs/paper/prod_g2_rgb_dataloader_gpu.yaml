version: 1
selection:
  scenario: image-rgb
  mode: pipeline
  libraries: [torchvision, kornia]
  transform_set: paper
data:
  data_dir: /tmp/unused
  gcs_uri: gs://example-bucket/datasets/imagenet/val.tar
  num_items: 10000
output:
  output_dir: gcp_runs/prod-g2-standard-16-rgb-dataloader-gpu-memory
  gcs_results_uri: gs://example-bucket/benchmark-runs
execution:
  pipeline_scope: memory_dataloader_augment
  num_runs: 1
  batch_size: 256
  workers: 8
  min_time: 0
  slow_preflight_items: 256
  thread_policy: pipeline-default
  device: cuda
cloud:
  provider: gcp
  project: example-project
  zone: us-central1-a
  machine_type: g2-standard-16
  disk_size_gb: 200
