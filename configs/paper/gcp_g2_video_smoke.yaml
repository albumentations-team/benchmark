version: 1
selection:
  scenario: video-16f
  mode: micro
  libraries: [torchvision, kornia]
  transform_set: paper
data:
  data_dir: /tmp/unused
  gcs_uri: gs://example-bucket/datasets/ucf101.tar
  num_items: 10
output:
  output_dir: gcp_runs/g2-standard-16-video-micro-smoke
  gcs_results_uri: gs://example-bucket/benchmark-runs
execution:
  num_runs: 1
  device: cuda
cloud:
  provider: gcp
  project: example-project
  zone: us-central1-b
  machine_type: g2-standard-16
  disk_size_gb: 200
