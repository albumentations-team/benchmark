version: 1
selection:
  scenario: video-16f
  mode: micro
  libraries: [albumentationsx, torchvision, kornia]
  transform_set: paper
data:
  data_dir: /path/to/videos
  num_items: 50
  clip_length: 16
output:
  output_dir: output/video_micro
execution:
  num_runs: 1
  device: none
