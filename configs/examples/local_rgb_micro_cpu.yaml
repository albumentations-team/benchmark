version: 1
selection:
  scenario: image-rgb
  mode: micro
  libraries: [albumentationsx, torchvision, kornia, pillow]
  transform_set: paper
data:
  data_dir: /path/to/imagenet/val
  num_items: 1000
output:
  output_dir: output/rgb_micro
execution:
  num_runs: 1
  device: none
